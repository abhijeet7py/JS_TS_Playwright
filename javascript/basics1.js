// To print something on the console we use console.log() function

console.log("Hello World!");

// This is single line comment

/*
This is multi line comment
*/

// In JavaScript we have 7 data types
let a = 4
console.log(a)

console.log(typeof(a)) // typeof is a operator which gives the data type of the variable

let b = 234.12
console.log(typeof(b))

let c = "Abhijeet"
console.log(typeof(c))

let d = true
console.log(typeof(d))

// let c = a + b (It will not work because We cannot redclare a variable with let keyword but we can with var)

c = a + b // It will work because we are not we are just reassigning a new value to it
console.log(c)

// We cannot redclare a variable with let keyword but we can with var

console.log(!d)

// ! is not operator which gives the opposite of the boolean value